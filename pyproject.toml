[project]
name = "agent-core"
version = "0.1.0"
description = "Repo-agnostic task loop for autonomous agentic coding"
requires-python = ">=3.11"
dependencies = ["pyyaml>=6.0"]

[dependency-groups]
dev = ["pytest>=8.0"]
